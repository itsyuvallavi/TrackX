{
  "name": "@trackx/webhook",
  "version": "0.0.0",
  "private": true,
  "description": "Cloudflare Worker webhook for Telegram messages.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @trackx/shared build && vitest run",
    "typecheck": "pnpm --filter @trackx/shared build && tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250605.0",
    "typescript": "^5.9.2",
    "vitest": "^4.0.0",
    "wrangler": "^4.20.0"
  },
  "dependencies": {
    "@trackx/shared": "workspace:*",
    "zod": "^4.2.0"
  }
}
